{
  "type": "object",
  "required": ["id", "chat_id", "sender_id", "type"],
  "properties": {
    "id": { "type": "string" },
    "chat_id": { "type": "string" },
    "sender_id": { "type": "string" },
    "type": { "type": "string", "enum": ["text", "appointment"] }
  }
}
