{
  "type": "object",
  "required": ["notifications", "unread"],
  "properties": {
    "unread": { "type": "integer" },
    "notifications": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "is_read"],
        "properties": {
          "id": { "type": "string" },
          "is_read": { "type": "boolean" }
        }
      }
    }
  }
}
