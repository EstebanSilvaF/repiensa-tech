{
  "type": "object",
  "required": ["id", "name", "email_domain", "subscription_status"],
  "properties": {
    "id": { "type": "string" },
    "name": { "type": "string" },
    "email_domain": { "type": "string" },
    "subscription_status": { "type": "string", "enum": ["active", "inactive", "expired"] }
  }
}
