/**
 * Prerrequisitos antes de correr Cypress E2E:
 * 1. Backend activo: npm run dev:backend (puerto 3000)
 * 2. BD con seed: npm run db:seed desde backend/ (o db:reset)
 * 3. Frontend: npm run dev (modo interactivo) o se levanta solo con npm run test:e2e
 */
import './commands';
